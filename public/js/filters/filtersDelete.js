async function deleteLivreFilters(id) {
    deleteLivre(id);
}

async function deleteAuteurFilters(id) {
    deleteAuteur(id);
}

async function deleteGenreFilters(id) {
    deleteGenre(id);
}

async function deleteUtilisateurFilters(id) {
    deleteUtilisateur(id);
}